<%= form_for(@atfal_event) do |f| %>
  <% if @atfal_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@atfal_event.errors.count, "error") %> prohibited this atfal_event from being saved:</h2>

      <ul>
      <% @atfal_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, 'Programm' %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :place, "Platz" %>
    <%= f.text_field :place %>
  </div>
  <div class="field">
    <%= f.label :startime, "Beginn" %>
    <%= f.datetime_select :startime %>
  </div>
  <div class="field">
    <%= f.label :endtime, "Ende" %>
    <%= f.datetime_select :endtime %>
  </div>
  <div class="actions">
    <%= f.submit 'Sichern', class: "btn btn-primary" %>
  </div>
<% end %>
